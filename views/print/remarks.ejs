<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Remarks</title>
    <style>
        .remarks {
            margin-top: 20px;
        }
        .remarks textarea {
            width: 100%;
            height: 100px;
        }
        .remarks-list {
            margin-top: 20px;
        }
        .remarks-list div {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <% if (success_msg) { %>
        <div class="alert alert-success"><%= success_msg %></div>
    <% } %>
    <% if (error_msg) { %>
        <div class="alert alert-danger"><%= error_msg %></div>
    <% } %>

    <div class="container">
        <form action="/print/saveRemarks" method="POST">
            <h1>Manage Remarks</h1>
            <div>
                <label for="teacherRemarks">Teacher's Remarks:</label>
                <textarea name="teacherRemarks" id="teacherRemarks"><%= settings.teacher_remarks %></textarea>
            </div>
            <div>
                <label for="cutOffPoint">Cut Off Point:</label>
                <input type="number" name="cutOffPoint" id="cutOffPoint" value="<%= settings.cut_off_point %>">
            </div>
            <div>
                <label for="promotedClass">Promoted Class:</label>
                <input type="text" name="promotedClass" id="promotedClass" value="<%= settings.promoted_class %>">
            </div>
            <div>
                <label for="repeatedClass">Repeated Class:</label>
                <input type="text" name="repeatedClass" id="repeatedClass" value="<%= settings.repeated_class %>">
            </div>
            <div>
                <label for="schoolReopenDate">School Reopen Date:</label>
                <input type="date" name="schoolReopenDate" id="schoolReopenDate" value="<%= settings.school_reopen_date %>">
            </div>
            <div>
                <label for="scoreRemarks">Score Remarks:</label>
                <textarea name="scoreRemarks" id="scoreRemarks"><%= settings.score_remark %></textarea>
            </div>
            <button type="submit">Save Remarks</button>
        </form>

        <form action="/print/uploadSignatureImage" method="POST" enctype="multipart/form-data">
            <div>
                <label for="signatureImage">Upload Signature Image:</label>
                <% if (signatureImageUrl) { %>
                    <div>
                        <img src="/<%= signatureImageUrl %>" alt="Current Signature" style="max-width: 100px; display: block; margin-bottom: 10px;">
                    </div>
                <% } %>
                <input type="file" id="signatureImage" name="signatureImage" accept="image/*">
            </div>
            <button type="submit">Upload Signature Image</button>
        </form>

        <div class="remarks-list">
            <h2>Teacher's Remarks</h2>
            <div>
                <p><%= settings.teacher_remarks %></p>
            </div>
        </div>
    </div>
</body>
</html>
