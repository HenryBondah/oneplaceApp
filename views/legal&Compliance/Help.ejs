<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <style>
        /* Add some basic styling */
        .form-group {
            margin-bottom: 15px;
        }
        .alert-success, .alert-error {
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 5px;
        }
        .alert-success {
            color: green;
            background-color: #d4edda;
        }
        .alert-error {
            color: red;
            background-color: #f8d7da;
        }
        .contact-info {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <h1>Contact Us</h1>

    <% if (success && success.length > 0) { %>
        <div class="alert-success"><%= success %></div>
    <% } %>
    <% if (error && error.length > 0) { %>
        <div class="alert-error"><%= error %></div>
    <% } %>

    <div class="contact-info">
        <p>For a quicker response, please visit our <a href="/legal/faq">FAQ page</a> or visit our <a href="https://www.youtube.com/">YouTube Page</a> for an easy step by step process of each action in one place..</p>
        <p>otherwise contact Support Phone: +1 (555) 123-4567</p> 
    </div>

    <form action="/legal/help" method="POST">
        <div class="form-group">
            <label for="name">Help Seekers Full Name:</label>
            <input type="text" id="name" name="name" required>
        </div>
        <div class="form-group">
            <label for="organization">Which Organization do you belong to:</label>
            <input type="text" id="organization" name="organization">
        </div>
        <div class="form-group">
            <label for="phone">Phone:</label>
            <input type="tel" id="phone" name="phone" required>
        </div>
        <div class="form-group">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required>
        </div>
        <div class="form-group">
            <label for="message">Message:</label>
            <textarea id="message" name="message" required></textarea>
        </div>
        <button type="submit">Seek Help</button>
    </form>
</body>
</html>
