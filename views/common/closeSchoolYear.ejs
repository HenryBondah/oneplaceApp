<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            font-family: Arial, sans-serif;
        }

        .close-school-year-container {
            width: 80%;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
            font-size: 28px;
        }

        h2 {
            color: #555;
            margin-bottom: 10px;
            font-size: 22px;
        }

        .school-year-buttons {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            padding: 12px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #007BFF;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #0056b3;
        }

        .no-terms-message {
            color: #777;
            font-size: 16px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="close-school-year-container">
        <h1><%= title %></h1>
        <% if (schoolYears && schoolYears.length > 0) { %>
            <% schoolYears.forEach(schoolYear => { %>
                <h2><%= schoolYear.year_label %></h2>
                <div class="school-year-buttons">
                    <% if (schoolYear.terms.length > 0) { %>
                        <% schoolYear.terms.forEach(term => { %>
<a href="/common/termDetails/<%= term.term_id %>" class="btn"><%= term.term_name %></a>
                        <% }); %>
                    <% } else { %>
                        <p class="no-terms-message">No terms available for this school year.</p>
                    <% } %>
                </div>
            <% }) %>
        <% } else { %>
            <p>No school years available.</p>
        <% } %>
    </div>
</body>
</html>
