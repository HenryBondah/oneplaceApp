<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Add Class and Subject</title>
    <style>
        .link-to-manage-class-subject {
            text-align: center;
            margin-bottom: 20px;
        }
        .link-to-manage-class-subject a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .link-to-manage-class-subject a:hover {
            background-color: #0056b3;
        }
        .separator {
            border-bottom: 1px solid #ccc;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <!-- Add the link to the Manage Class, Subject, and Graduation Year page -->
    <div class="link-to-manage-class-subject">
        <a href="/common/manageClassSubjectAndGradYr">Back</a>
    </div>

    <!-- Horizontal line separator -->
    <div class="separator"></div>

    <main class="add-class-subject-container">
        <h1>Add Class and Subject</h1>
        <div class="messages">
            <% if (messages.success) { %>
                <p class="success"><%= messages.success %></p>
            <% } %>
            <% if (messages.error) { %>
                <p class="error"><%= messages.error %></p>
            <% } %>
        </div>
        <div class="form-section">
            <form action="/common/addClass" method="POST">
                <h2>Add Class</h2>
                <label for="className">Class Name:</label>
                <input type="text" id="className" name="className" required>
                
                <label for="gradYearGroupId">Graduation Year Group:</label>
                <select id="gradYearGroupId" name="gradYearGroupId">
                    <% availableGradYearGroups.forEach(function(group) { %>
                        <option value="<%= group.id %>"><%= group.name %></option>
                    <% }); %>
                </select>

                <button type="submit">Add Class</button>
            </form>
        </div>
        <div class="form-section">
            <form action="/common/addSubject" method="POST">
                <h2>Add Subject</h2>
                <label for="subjectName">Subject Name:</label>
                <input type="text" id="subjectName" name="subjectName" required>
                
                <label for="classId">Class:</label>
                <select id="classId" name="classId">
                    <% classes.forEach(function(classItem) { %>
                        <option value="<%= classItem.class_id %>"><%= classItem.class_name %></option>
                    <% }); %>
                </select>

                <button type="submit">Add Subject</button>
            </form>
        </div>
        <div class="form-section">
            <form action="/addGraduationYearGroup" method="POST">
                <h2>Add Graduation Year Group</h2>
                <label for="graduationYear">Graduation Year:</label>
                <input type="number" id="graduationYear" name="graduationYear" required>
                <button type="submit">Add Graduation Year Group</button>
            </form>
        </div>
    </main>
</body>
</html>
