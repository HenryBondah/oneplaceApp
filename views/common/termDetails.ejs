<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>  <!-- Title is now defined -->
    <style>
        body {
            background-color: #ffffff;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            width: 80%;
            margin: 20px auto;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #333;
            text-align: center;
            font-size: 28px;
            margin-bottom: 15px;
        }

        .term-details {
            text-align: center;
            font-size: 18px;
            margin-bottom: 30px;
        }

        .term-details h3 {
            margin: 5px 0;
        }

        .classes-container {
            text-align: center;
        }

        h2 {
            font-size: 24px;
            margin-bottom: 15px;
        }

        .class-item {
            background-color: #f1f1f1;
            padding: 10px 15px;
            border-radius: 6px;
            border: 1px solid #ddd;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: background-color 0.3s ease;
        }

        .class-item:hover {
            background-color: #e9e9e9;
        }

        .btn {
            padding: 8px 12px;
            font-size: 14px;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="classes-container">
            <h1><%= title %></h1>
        <div class="term-details">
            <h3>Term: <%= term.term_name %></h3>
            <h3>Start Date: <%= new Date(term.start_date).toLocaleDateString() %></h3>
            <h3>End Date: <%= new Date(term.end_date).toLocaleDateString() %></h3>
        </div>
            <h2>Classes</h2>
            <% if (classes && classes.length > 0) { %>
                <% classes.forEach(function(cls) { %>
                    <div class="class-item">
                        <span><%= cls.class_name %></span>
                        <a href="/print/printStudentReport?classId=<%= cls.class_id %>&termId=<%= termId %>" class="btn">Generate Report</a>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="class-item">No classes assigned for this term</div>
            <% } %>
        </div>
    </div>
</body>
</html>
